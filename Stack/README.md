This stack ADT uses array based implementation. 
The stack will be initialized by specifying the size of the array the stack will be based on.
Once the array hits the capacity limit, it shows the error message and ignore the push call. 